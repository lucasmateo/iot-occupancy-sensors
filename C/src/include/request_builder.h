/*
 * build a correct http request with the given parameters
 * therequest param is where the reauest will be stored
 * only has string parsing value
 */
void build_request(char* request,const char* method,const char* path,const char* webserver,const char* content);
